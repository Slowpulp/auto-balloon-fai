---
name: auto-balloon-fai-v2
description: "从零件工程图 PDF 和检验模板生成工业 FAI 气泡图及对应检验表。默认将每次任务隔离为独立新项目；仅在用户明确指定基线时处理历史编号修订。用于首件检验、来料检验、机加工、钣金和医疗器械零部件；不用于普通 Excel 气泡图。"
---

# 自动气泡图 V2

从同一份受控特性台账生成图纸气泡标注和检验记录，确保气泡号、检验行和检具映射一一对应。

## 项目模式与隔离

- 每次任务默认使用 `independent` 模式，视为独立新项目。只使用用户在当前任务中明确提供或指定的输入，不主动搜索、读取或复用其他项目、父级或同级目录中的旧台账、旧气泡图、旧坐标、旧交付物或历史版次。
- 源图哈希相同只证明文件内容一致，不证明属于同一项目或同一修订链，也不构成复用历史数据的授权。不得从跨会话记忆、相似文件名、相同零件号或自动发现的哈希匹配推断用户希望继承基线。
- 只有用户在当前任务中明确指定具体基线文件或目录，并明确要求保持历史编号、坐标或交付版次时，才使用 `revision` 模式。用户只说“修订”但没有指定基线时，先请求基线或确认改用独立模式。
- 在内部台账记录 `project_mode`、本次输入范围和模式依据。`revision` 模式还要记录用户指定的基线路径、基线哈希与授权内容；不得把“找到旧文件”写成授权依据。

## 默认交付契约

除非用户明确要求其他内容，只交付：

1. 一份 FAI 气泡图 PDF。
2. 一份 XLSX，且只含两个工作表，顺序固定为 `FAI检验记录`、`检具代码`。

不生成用户交付用的独立修改日志，不在工作簿中增加特性清单、图纸校验、公差依据、修改日志或隐藏审计工作表。内部台账、QA 文件以及项目环境要求的 Git/CHANGELOG 版本记录只放在工作目录，不作为交付物。

交付说明必须写明本次使用 `independent` 还是 `revision` 模式。使用 `revision` 模式或偏离默认编号、禁区、公差规则时，还要简要列出用户明确指定的基线和批准决定；这些说明不另建用户交付文件或工作表。

“两页检验表”默认解释为两个工作表。只有用户明确要求物理打印共两页时，才把打印页数作为额外验收条件，并通过目标办公软件导出 PDF 核验。

## 开始前

- 先确定项目模式与输入边界。`independent` 模式禁止基线侦察；`revision` 模式只读取用户明确指定的基线。
- 完整渲染并检查源图和模板；记录文件哈希、页数、页面尺寸、旋转和修订信息。
- 零件图纸控制零件身份、材料、几何、显式公差和技术要求。模板只控制表单布局和术语；只有被用户或模板明确标识为受控字典的检具代码才能复用，普通示例行不得复制。不得带入模板中的示例零件数据、实测值或签字。
- `revision` 模式继承历史 ID 前，先逐项确认目标语义仍然对应并通过当前范围/禁区审计。直接复用旧气泡坐标还要求对应源图哈希、页面几何和旋转完全一致；坐标条件不满足时重新定位，不能仅凭旧坐标推断语义。任何未解决的映射或规则冲突都转入审查。
- 使用 PDF 工作流处理图纸，用 Artifact Tool 创建或编辑 XLSX，并遵守各自的渲染与验证要求。

## 工作流程

1. 确定 `independent` 或 `revision` 模式及允许读取的输入范围。详细步骤见 [workflow.md](references/workflow.md)。
2. 建立唯一特性台账，再制作任何交付物。
3. 识别全部需要检测的尺寸、形位公差和独立特征；明确重复特征是合并为数量还是逐个编号。
4. 定义硬禁区并布置气泡和引线。技术要求区、标题栏、修订栏和行政表格默认不编号，圆圈、整条引线和箭头都不得进入当前项目的硬禁区。
5. 对显式公差原样建限；遇到未注公差、专用特征或冲突时，按 [tolerance-governance.md](references/tolerance-governance.md) 处理。
6. 从同一台账生成气泡 PDF 和两表 XLSX。工作簿结构与判定逻辑见 [inspection-workbook.md](references/inspection-workbook.md)。
7. 执行 [quality-gates.md](references/quality-gates.md) 中的自动检查和逐页视觉复核，全部通过后再交付。

## 编号与范围

- `independent` 模式从零提取特性，使用连续唯一的 `1..N`；误编号删除后闭合缺口，不得读取历史编号决定当前序列。
- `revision` 模式才保留用户指定基线中的既有 ID；新增项从历史最大 ID 后追加，退役 ID 不复用，允许有记录在案的历史缺号。只有用户明确批准整体重编号时才重排并输出映射。
- 继承 ID 前必须按当前规则完成范围与禁区审计。基线 ID 指向硬禁区内的内容时标记 `HOLD`，不得静默继承。合规默认处置是退役该 ID，并把仍需检验的内容移入未编号要求区；如果用户明确要求保留，必须由用户具体重定义本项目的禁区范围并记录依据，然后按重定义后的完整禁区集合重新执行质量门禁。
- 每个编号必须同时存在于台账、一个可见气泡、一条可见引线和检验表的一行中。
- 不编号不等于不检验。未编号的技术要求或局部文字可放在 `FAI检验记录` 的未编号要求区，NO. 留空。
- 文字说明不应误判为检测特性。例如“虚线区域不油漆”保留原文且不编号；若与另一条要求冲突，两条都保留并标记 HOLD。
- 用户删除误编号项目时，同步更新气泡、引线、台账、检验行和所有引用。`independent` 模式闭合编号缺口；`revision` 模式把历史 ID 记为退役且永不复用。

## 气泡布置

- 气泡使用高对比圆圈、白色不透明底和可见引线，箭头落在目标尺寸或特征框旁，不覆盖数字、符号或公差框。
- 气泡完整圆形、整条引线走廊和箭头均需避开当前项目硬禁区与源图文字。只检查圆心、端点或目标文字不够。
- 自动碰撞通过后仍要逐页高分辨率人工确认锚点语义正确，尤其是相邻尺寸、重复视图和断面图。

## 放行边界

- 不伪造实测值、批次、检具型号、资产号、校准证据、批准或签字。
- 项目模式不明、基线授权缺失或继承范围与当前禁区规则冲突时，状态保持 `HOLD`，不得声称全部质量门禁通过。
- 默认公差未批准、专用特征定义不完整、要求冲突未关闭或实际检具信息缺失时，状态保持 `FOR REVIEW`/`HOLD`。
- 结构验证通过只证明文件生成正确，不代表零件合格或已放行。

## 输出命名

遵循用户约定；未另行指定时使用：`年月日_主题内容（实际模型名称_Vn）.格式`。

- `实际模型名称` 必须来自当前任务的运行时模型标识，并转换为正式的人类可读名称；例如运行时 `gpt-5.6-sol` 写作 `GPT-5.6 Sol`。不得用 `Codex`、`ChatGPT`、`AI` 等产品名或泛称代替，也不得把用户给出的示例名称当成未经核实的实际模型。
- `Vn` 表示同一零件/图纸交付包的对外版次。`independent` 模式首次交付使用 `V1`，不得扫描其他项目推断下一版次；`revision` 模式只根据用户指定基线的已发布序列确定 `V2`、`V3`。不得把项目语义版本号写成对外交付版次；仅修正文件名且交付内容未形成新一版时，保留原 `Vn`。
- 项目内部语义版本、图纸修订号和对外交付版次分别记录，互不替代。图纸修订号继续保留在 PDF 元数据和检验表的独立字段中。
